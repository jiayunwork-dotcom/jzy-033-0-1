"""TCP CUBIC 拥塞窗口核算服务。

模块按职责拆分：

- ``cubic``      : 窗口演进的数学内核（纯函数 / 纯计算，无 HTTP 依赖）
- ``errors``     : 带类型的结构化领域错误
- ``validation`` : 入参校验，在开算前挡下非法输入
- ``service``    : 单点求值与轨迹推进的编排，两个入口共用同一套内核
- ``presets``    : 内置标准情形（只读回显 + 预置核对算例）
- ``main``       : FastAPI 应用与 HTTP 路由
"""

__version__ = "1.0.0"
