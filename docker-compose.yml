services:
  cubic-service:
    build: .
    image: cubic-window-service:latest
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import json,urllib.request; r=urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=2); assert json.load(r)['status']=='ok'"]
      interval: 15s
      timeout: 3s
      retries: 3
